<p class="fields">
  <%= f.collection_select(:developer_account_id, Developer.all, :id, :name, { include_blank: true }) %>
  <%= f.hidden_field :_destroy %>
  <%= link_to "remove", "#", onclick: "return remove_fields(this);" %>
</p>
